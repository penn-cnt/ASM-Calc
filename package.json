{
    "private": true,
    "workspaces": [
        "app"
    ],
    "scripts": {
        "dev": "vercel dev",
        "build": "cd app && npm install && npm run build && mkdir -p ../dist && cp -r dist/* ../dist/",
        "vercel-build": "cd app && npm install && npm run build && mkdir -p ../dist && cp -r dist/* ../dist/"
    }
}